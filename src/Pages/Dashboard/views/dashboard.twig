{% extends 'template.html.twig' %}

{% block navigation %}
    {% include 'navigation.logged.twig' %}
{% endblock navigation %}


{% block content %}
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <!-- Header -->

        <div class="mb-8 hidden" id="userName">
            <h2 class="text-3xl font-bold text-gray-900">Welcome back, userName !</h2>
            <p class="text-gray-600 mt-1">Here's what's happening with your links today</p>
        </div>

        {% include 'dashboard.total.twig' %}

        <!-- Ad Space
        <div class="mb-8">
            <div class="bg-gray-200 border-2 border-dashed border-gray-300 rounded-xl p-8 text-center">
                <p class="text-sm text-gray-500 font-medium">Advertisement Space</p>
                <p class="text-xs text-gray-400 mt-1">728 x 90</p>
            </div>
        </div>
        -->

        {% include 'dashboard.recents.links.twig' %}

        {#  {% include 'dashboard.activity.twig' %}
        {% include 'dashboard.geographic.twig' %} --> #}


        <!-- Ad Space Bottom -->
        <div class="mt-8">
            <div class="bg-gray-200 border-2 border-dashed border-gray-300 rounded-xl p-12 text-center">
                <p class="text-sm text-gray-500 font-medium">Advertisement Space</p>
                <p class="text-xs text-gray-400 mt-1">970 x 250</p>
            </div>
        </div>
    </div>

{% endblock content %}
{% block scripts %}
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="./javascript/user.js"> </script>
    <script src="./javascript/dashboard.js"> </script>
{% endblock %}