
    <nav class="bg-white border-b border-gray-200 sticky top-0 z-40">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center space-x-8">
                    <h1 class="text-2xl font-bold text-orange-500">ShortLink</h1>
                    <div class="hidden md:flex space-x-6">
                        <a href="/dashboard" class="text-orange-500 font-medium text-sm border-b-2 border-orange-500 pb-1">Dashboard</a>
                        <a href="/links" class="text-gray-600 hover:text-gray-900 text-sm">Links</a>
                        <a href="/qr-code" class="text-gray-600 hover:text-gray-900 text-sm">QR Codes</a>
                        <a href="/api-documentation" class="text-gray-600 hover:text-gray-900 text-sm">Api Documentation</a>
                    </div>
                </div>
                <div class="flex items-center space-x-4">
                    <button class="bg-blue-600 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-blue-700" id="createNewUrl">
                        Create New Short Url
                    </button>
                    <button class="bg-blue-600 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-blue-700" id="logout">
                        Logout
                    </button>
                </div>
            </div>
        </div>
    </nav>

    <script>
        (function(){
            document.querySelector('#createNewUrl').addEventListener('click' , function (event) {
                event.preventDefault();
                location.assign('/create-new-url');
            })

            document.querySelector('#logout').addEventListener('click' , function (event) {
                event.preventDefault();
                location.assign('/logout');
            })

        })()
    </script>
